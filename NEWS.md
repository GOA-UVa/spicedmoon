# NEWS

- times can be passed as datetimes instead of just as strings.
- tried to avoid a furnsh_c rare error
- The user can choose to use bodvrd to obtain the Moon's radios instead of using spicedmoon default values
- The source_frame and target_frame of the EarthLocations can be manually set instead of using ITRF93
